server:
  port: 8001
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin
          uri: https://github.com/santiagoforonda/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-server