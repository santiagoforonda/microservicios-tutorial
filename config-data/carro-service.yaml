server:
  port: 8082
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      deafult-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost